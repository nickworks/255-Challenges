package  {
	import flash.display.MovieClip;
	import flash.events.MouseEvent;

	public class Game extends MovieClip {
		
		public function Game() {
			// TODO: play a sound effect when the player clicks on the lighter, background circle
			// Do NOT play a sound effect when the player clicks on the darker, foreground circle
		}
	}
}
