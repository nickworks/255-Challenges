Outcome: Student can calculate delta-time and utilize it to create time-based animation.


1. In Game.calcDeltaTime(), calculate and return delta-time (in seconds).

2. In Game, make any necessary modifications to this class to support the calculation and use of delta-time.

3. In Circle.update(), modify this method to use delta-time.

4. In Circle.update(), make the object move to the right at a rate of 200 pixels-per-second.