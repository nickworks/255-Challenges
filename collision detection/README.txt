Outcome: Student can implement radial (circular) collision detection.

1. In Game.checkCollision(), check if Circle one overlaps Circle two.
	- If overlapping occurs, return true
	- Otherwise, return false
	- Do NOT use built-in methods like MovieClip.hitTest()