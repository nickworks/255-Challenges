Outcome: Student can create vectors and calculate the cartesian and polar coordinates of any vector.

1. In Game.handleFrame(), calculate angles for pointer1 and pointer2; set their rotation properties