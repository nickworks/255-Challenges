Outcome: Student can calculate delta-time and utilize it to create count-down timers.

1. In Game.calcDeltaTime(), calculate and return delta-time (in seconds).
2. In Game, make any necessary modifications to this class to support the calculation and use of delta-time.
3. In Game.countdown(), modify this method to call the warp() method every 3 seconds. Use the timer property.