package  {	import flash.display.MovieClip;	import flash.events.Event;	/**	 * This defines the document-level class.	 */	public class Game extends MovieClip {				/**		 * This constructor sets up the ENTER_FRAME event listener for this object.		 */		public function Game() {			addEventListener(Event.ENTER_FRAME, handleFrame);		}		/**		 * This method uses the mouse position to move the dot object.		 * @param e:Event	The Event object that called this event handler.		 */		private function handleFrame(e:Event):void {			/* START: Move the dot to be positioned in the middle of whatever "cell"			 * the mouse is currently hovering over.			 */			/* END */		}	}	}